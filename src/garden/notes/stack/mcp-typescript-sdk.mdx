[github.com/modelcontextprotocol/typescript-sdk](https://github.com/modelcontextprotocol/typescript-sdk) · TypeScript

I wanted tool-use to be a protocol rather than a bespoke JSON contract
rewritten for every app. Writing a tool once and having any client speak to it
is the whole point, and it is what MCP tool-use in production actually buys.
