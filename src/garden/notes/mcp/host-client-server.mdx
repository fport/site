Three roles, and they are easy to blur. The **host** is the application. It
holds one **client** per connection. Each client talks to one **server**.

<Diagram
  title="one client per server"
  nodes={[
    { id: 'c1', label: 'MCP client 1', sub: 'one connection', kind: 'service', x: 0, y: 0 },
    { id: 'c2', label: 'MCP client 2', sub: 'one connection', kind: 'service', x: 0, y: 1 },
    { id: 's1', label: 'MCP server', sub: 'files · local', kind: 'service', x: 1, y: 0 },
    { id: 's2', label: 'MCP server', sub: 'issues · remote', kind: 'service', x: 1, y: 1 },
    { id: 'db', label: 'Database', sub: 'local or remote', kind: 'data', x: 2, y: 0 },
    { id: 'web', label: 'Web API', sub: 'some remote service', kind: 'cloud', x: 2, y: 1 },
  ]}
  groups={[
    { label: 'Host · IDE, chat client, agent', kind: 'external', nodes: ['c1', 'c2'] },
    { label: 'the part you write', kind: 'security', nodes: ['s1', 's2'] },
  ]}
  edges={[
    { from: 'c1', to: 's1', label: 'stdio', kind: 'service' },
    { from: 'c2', to: 's2', label: 'Streamable HTTP', kind: 'service' },
    { from: 's1', to: 'db', label: 'SQL', kind: 'data' },
    { from: 's2', to: 'web', label: 'HTTPS', kind: 'cloud', dashed: true },
  ]}
/>

<Table
  data={{
    headers: ['Role', 'Who writes it', 'What it owns'],
    rows: [
      ['Host', 'the app vendor', 'the model, the UI, the approval prompts'],
      ['Client', 'the SDK', 'one connection, protocol bookkeeping'],
      ['Server', 'you', 'capabilities and the external system behind them'],
    ],
  }}
/>

The server is the only piece most people write. It exposes capabilities and
does the talking to whatever sits behind it — a local database, a web API, a
filesystem.

<Callout type="note" title="One client per server">
  A host with five servers holds five clients. That isolation is deliberate: a
  compromised or broken server cannot see the others' traffic.
</Callout>
